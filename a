edit id ga


const player = team.players.find(
        item => item.id === editId
    );


deletega


     team.players = team.players.filter(
        item => item.id !== editId
    );


add player btn ga

    editId = null;

playerName.value = "";
playerNum.value = "";
playerPosition.value = "";

playerModalTitle.textContent = "Add Player";

playerDelete.classList.add("hidden");\













for (let i = arr.length - 1; i >= 0; i--) {
    console.log(arr[i]);
}